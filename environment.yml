name: ecg-heartbeat
channels:
  - defaults
dependencies:
  - pip:
    - -e .
    - ipykernel
    - click
    - Sphinx
    - coverage
    - flake8
    - pandas 
    - matplotlib
    - seaborn
    - torch
    - python-dotenv>=0.5.1
    - torchmetrics
    - mlflow
    - hyperopt
    - pre-commit
    - tqdm
    - lightning
    - PyWavelets
prefix: /opt/anaconda3/envs/ecg-heartbeat
